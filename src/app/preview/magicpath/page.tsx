import Landing from "@/components/marketing/landing/Landing";

/* Design sandbox for the production landing ("/"). Same component,
   no auth redirect, so it stays previewable while signed in. */
export default function MagicpathPreview() {
  return <Landing />;
}
